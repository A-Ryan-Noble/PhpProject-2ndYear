{% extends '_template.html.twig' %}

{% if username == 'admin' %}

    {% block adminAccountsPage %} current_page {% endblock %}

{% endif %}

{% if username == 'staff' %}

    {% block staffAccountsPage %} current_page {% endblock %}

{% endif %}

{% block main %}

    <div class = "column_container">

        <section class = "top">

            <h4>Will try and delete the Visitor.</h4>

        </section>

    </div>

    <div class = "column_container">

        <section class = "middle">

            <table>

                <tr>
                    <th id="titleRow">ID</th>
                    <th id="titleRow"> Name</th>
                    <th id="titleRow">Email</th>
                    <th  id="titleRow">Password</th>
                </tr>

                <tr>
                    <th>{{ visitor.id }}</th>
                    <th>{{ visitor.aName }}</th>
                    <th>{{ visitor.aEmail }}</th>
                    <th>{{ visitor.aPassword }}</th>
                </tr>

            </table>

            <br>

            <input type="submit" value="Delete" onclick="location.href='index.php?action=visitorDelete'">

        </section>

    </div>

{% endblock %}
